# Archived STATE narrative (epoch:1783219303) — finished phases

Trimmed from agents/STATE.md at the bench-optimization phase boundary (spine was
55% of the upfront budget). Full history: git log on agents/STATE.md.

## Definition of Done (all shipped)
- [x] Loop-engineering v2 (Phase A–E) shipped + đo bằng `8sync harness bench`
- [x] `/gs` — một lệnh chạy team tự động; `auto` không dừng; hint `[auto|<goal>|status|next|stop]`; QA+Closeout gate
- [x] Bare `8sync harness` = auto-setup đầy đủ (MCP + skills + /gs + memory + inject + index)
- [x] Doc-hygiene code-backed (`8sync harness audit`) + wired vào `doctor` + `/gs` (v0.22.0)
- [x] AI-engine health check trong `doctor` (v0.22.0)
- [x] Loop correctness — codegraph verbs đúng · force-load dedup · impeccable path fix (v0.22.0)
- [x] Loop quality probe `8sync harness eval` — verified 3/3 (v0.23.0)
- [x] /gs L3 worktree isolation: `git worktree add .gs/wt/<slug> -b gs/<slug>` (v0.23.0)
- [x] Dashboard redesign + Models/Projects (v0.29.0): full impeccable redesign · Models page · project switcher · markdown render · serena-off fix · context honesty · workflow canvas — 14 pages, 0 console errors.

## Local GGUF models (Unreleased, 8/8 tasks done, commit a9140c2)
Runner: **mistral.rs** — pure-Rust GGUF loader with OpenAI `/v1` server; prebuilt CUDA/CPU binary.
- Slice 1 — `8sync harness add-local-model <path> [name]` (`harness/local_model.rs`):
  classify path (local .gguf | HF repo | URL, GGUF magic-checked) → ensure mistralrs →
  systemd user service `8sync-llm-<name>` → register omp provider `local/<name>` in
  models.yml (sentinel block; TSV registry source-of-truth) → verify `/v1`. `list`/`rm`.
- Slice 2 — coexist+wire: `gateway apply` preserves the local block; `doctor` +
  `capabilities.md` report local models; help/README/CHANGELOG/KNOWLEDGE.
- Slice 3 — LocateAnything-3B: `8sync locate <img> <prompt>` via mudler/locate-anything.cpp
  (MIT ggml port, prebuilt q8_0 GGUF on HF) → boxes JSON; `--setup` builds the CLI.
- Slice 4 — closeout: real E2E on this box — mistral.rs 0.8.23 (auto `cuda131-sm120`
  RTX 5080) serves SmolLM2-135M GGUF. Caught+fixed 2 bugs: serve needs
  `-m <dir> -f <file> --format gguf`; omp `id: default` + `local/<name>` via `name:` field.

## Earlier this session (Unreleased, shipped)
1. **zai-vision MCP + skill** — auto-install `@z_ai/mcp-server`, `Z_AI_VISION_MODEL=glm-4.6v-flash`
   (only free/working vision model on stock key, verified via real stdio JSON-RPC), key from
   `omp token zai`; skill documents the combination matrix. `~/.omp/capabilities.md` + doctor.
2. **kitty window decorations** — `hide_window_decorations yes` broke title bar on stacking WMs;
   `env_detect::is_tiling_wm()` gates it to Hyprland/HyDE/sway. Needs kitty window reopen.
3. **exact MCP tool catalogs** — `capabilities.md` embeds the FULL tool list per registered MCP
   server + omp built-ins (parsed live from `omp --help`) + Mnemopi tools.
4. **kitty zoom binding** — vsplit moved `ctrl+shift+minus` → `ctrl+shift+backslash`.
5. **18 feynman research skills ported omp-native** — 14 were dead stubs (feynman slash-commands),
   4 cosmetic; all ported into `assets/skills/<name>/SKILL.md`. `alpha-research` kept on feynman
   (real CLI). Fixed `update_skills` collection re-install bug. `peer-review`→`research-review`.
